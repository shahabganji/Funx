name: Funx CI

on: 
  push:
    branches: 
      - master
      - develop

jobs:
  
  test:
    
    runs-on: ubuntu-latest
        
    steps:
      - uses: actions/checkout@v1
      - name: Setup .Net Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 5.0
      - name: Test the library
        run: dotnet test  tests/Funx/Funx.Tests.csproj
    
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0
    - name: Build with dotnet
      run: dotnet build --configuration Release
